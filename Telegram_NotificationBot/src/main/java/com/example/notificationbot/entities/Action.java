package com.example.notificationbot.entities;

/**
 * Enum representing the different actions that can be performed
 * within the notification bot.
 */
public enum Action {
  FREE, SENDING_TITLE, SENDING_DESCRIPTION, SENDING_TIME
}
